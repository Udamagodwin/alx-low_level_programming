#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * more headers goes there
 * main - A program that print alphabet alphabet digit
 * numbers in base 10 starting from
 * 0. followed by a new line
 *Return: Always 0
*/

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	printf('\n');
	return (0);
}
